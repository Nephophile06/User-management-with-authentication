{
  "name": "user-management-app",
  "version": "1.0.0",
  "description": "User Management Application with Authentication (JavaScript Version)",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "setup": "npm run install-all && echo 'Setup complete! Run npm run dev to start the application'"
  },
  "keywords": ["user-management", "authentication", "react", "express", "javascript"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 